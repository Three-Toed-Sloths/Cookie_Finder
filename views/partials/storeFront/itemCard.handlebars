<div class="col s12 l6">
    <div class="card item">
        <div class="card-image">
            <img src={{''}}> {{!--need to add image colomn to the table for a source--}}
        </div>
        <div class="card-content">
            <span class="card-title" data-id='{{id}}'>{{product_name}}</span>
            <p>{{description}}</p>
            <p>Price: ${{price}}</p>
            <a class="green waves-effect waves-light btn add-to-cart"><i class="material-icons left">local_grocery_store</i>Add to Cart</a>
        </div>
    </div>
</div>